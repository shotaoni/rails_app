<% provide(:title, 'All users') %>
<h1>ユーザ一覧</h1>
<div>
  <%= paginate @users, window: 10, outer_window: 1 %> 
</div>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>