<div class="container micropost-container">
  <h1>投稿</h1>
  <%= form_with(model: @micropost, url: microposts_path, local: true) do |form| %>
    <%= render 'shared/error_messages', object: form.object %>
    <div class="form-group">
      <%= form.text_area :content, class: 'form-control', placeholder: "内容" %>
    </div>
    <div class="form-group">
      <%= form.file_field :image %>
    </div>
    <div class="form-group">
      <%= form.submit "投稿する", class: 'btn btn-info btn-lg form-submit' %>
    </div>
  <% end %>
</div>